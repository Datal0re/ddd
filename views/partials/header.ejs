<header class="app-header" style="background: var(--bg-secondary); padding: 1rem 0; border-bottom: 1px solid var(--border-color); margin-bottom: 2rem;">
  <div class="container" style="display: flex; justify-content: space-between; align-items: center;">
    <div style="display: flex; align-items: center; gap: 1rem;">
      <h1 style="margin: 0; font-size: 1.5rem; color: var(--primary-blue);">
        <a href="/" style="text-decoration: none; color: inherit;">🔍 Data Dumpster Diver</a>
      </h1>
    </div>
    
    <% if (typeof user !== 'undefined' && user) { %>
      <div style="display: flex; align-items: center; gap: 1rem;">
        <a href="/profile" style="display: flex; align-items: center; gap: 0.5rem; color: var(--text-secondary); text-decoration: none; font-size: 0.875rem; padding: 0.25rem 0.75rem; border-radius: 0.375rem; transition: all 0.2s ease;" 
           onmouseover="this.style.background='var(--bg-tertiary)'; this.style.color='var(--text-primary)';"
           onmouseout="this.style.background='transparent'; this.style.color='var(--text-secondary)';">
          <span>👤</span>
          <span><%= user.username %></span>
        </a>
        <form action="/logout" method="post" style="margin: 0;">
          <button type="submit" class="secondary" style="padding: 0.25rem 0.75rem; font-size: 0.875rem;">
            🚪 Logout
          </button>
        </form>
      </div>
    <% } %>
  </div>
</header>
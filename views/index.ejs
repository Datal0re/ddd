<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Data Dumpster Diver</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    .page-header {
      text-align: center;
      margin-bottom: 3rem;
    }
    .page-header h1 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
      color: var(--text-primary);
    }
    .page-header p {
      font-size: 1.1rem;
      color: var(--text-secondary);
      margin-bottom: 2rem;
    }
    .status-indicator {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 500;
    }
    .status-indicator.has-data {
      background: var(--success-bg);
      color: var(--success-text);
    }
    .status-indicator.no-data {
      background: var(--secondary-color);
      color: white;
    }
    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: currentColor;
    }
    .actions-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 1.5rem;
      margin-top: 2rem;
    }
    .action-card {
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      cursor: pointer;
    }
    .action-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }
    .action-card.primary {
      border-color: var(--primary-color);
      background: linear-gradient(135deg, var(--card-bg) 0%, rgba(var(--primary-color-rgb), 0.05) 100%);
    }
    .action-card h3 {
      margin: 0 0 1rem 0;
      font-size: 1.3rem;
      color: var(--text-primary);
    }
    .action-card p {
      margin: 0 0 1.5rem 0;
      color: var(--text-secondary);
      line-height: 1.6;
    }
    .action-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      display: block;
    }
    .btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      border-radius: 6px;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.2s ease;
      border: none;
      cursor: pointer;
      font-size: 1rem;
    }
    .btn-primary {
      background: var(--primary-color);
      color: white;
    }
    .btn-primary:hover {
      background: var(--primary-color-dark);
      transform: translateY(-1px);
    }
    .btn-secondary {
      background: var(--secondary-color);
      color: white;
    }
    .btn-secondary:hover {
      background: var(--secondary-color-dark);
      transform: translateY(-1px);
    }
    .btn-outline {
      background: transparent;
      color: var(--primary-color);
      border: 2px solid var(--primary-color);
    }
    .btn-outline:hover {
      background: var(--primary-color);
      color: white;
    }
    .sessions-info {
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 1rem;
      margin-bottom: 2rem;
      text-align: center;
      font-size: 0.9rem;
      color: var(--text-secondary);
    }
    .hidden {
      display: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="page-header">
      <h1>Data Dumpster Diver</h1>
      <p>Explore and visualize your ChatGPT conversation data</p>
      <div class="status-indicator <% if (hasExistingData) { %>has-data<% } else { %>no-data<% } %>">
        <span class="status-dot"></span>
        <span><% if (hasExistingData) { %>Data available (<%= sessionsCount %> session<%= sessionsCount !== 1 ? 's' : '' %>)<% } else { %>No data found<% } %></span>
      </div>
    </div>

    <% if (hasExistingData) { %>
      <div class="sessions-info">
        You have <%= sessionsCount %> session<%= sessionsCount !== 1 ? 's' : '' %> of conversation data ready to explore
      </div>
    <% } %>

    <div class="actions-grid">
      <% if (hasExistingData) { %>
        <!-- Explore conversations card (primary when data exists) -->
        <div class="action-card primary fade-in">
          <span class="action-icon">🔍</span>
          <h3>Explore Conversations</h3>
          <p>Browse through your existing ChatGPT conversations and media assets</p>
          <a href="/conversations?session=<%= firstSessionId %>" class="btn btn-primary">Start Exploring</a>
        </div>

        <!-- Upload new data card (secondary) -->
        <div class="action-card fade-in">
          <span class="action-icon">📤</span>
          <h3>Upload New Data</h3>
          <p>Add a new ChatGPT export to your collection</p>
          <a href="/upload" class="btn btn-outline">Upload ZIP File</a>
        </div>
      <% } else { %>
        <!-- Upload card (primary when no data exists) -->
        <div class="action-card primary fade-in">
          <span class="action-icon">📤</span>
          <h3>Upload Your First Data</h3>
          <p>Get started by uploading your ChatGPT export file</p>
          <a href="/upload" class="btn btn-primary">Upload ZIP File</a>
        </div>

        <!-- Info card about how it works -->
        <div class="action-card fade-in">
          <span class="action-icon">ℹ️</span>
          <h3>How It Works</h3>
          <p>Export your data from ChatGPT (Settings → Data controls → Export), then upload the resulting zip file to explore your conversations in an organized interface.</p>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>